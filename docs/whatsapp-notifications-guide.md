# دليل إشعارات WhatsApp التلقائية

## نظرة عامة
تم إضافة نظام إشعارات WhatsApp التلقائي الذي يرسل تنبيهات فورية عندما يصل المخزون إلى نقطة إعادة الطلب.

## المميزات الرئيسية

### 1. الإشعارات التلقائية
- يتم إرسال إشعار WhatsApp تلقائياً عند تحديث المخزون إذا وصل المنتج لنقطة إعادة الطلب
- يتم فحص المخزون بعد كل عملية تحديث (بيع، شراء، تعديل)
- لا يتم إرسال إشعارات مكررة لنفس المنتج خلال 24 ساعة

### 2. الإعدادات المرنة
- تفعيل/تعطيل النظام بالكامل
- إضافة أرقام هواتف متعددة لتلقي الإشعارات
- تخصيص قالب الرسالة
- إرسال ملخص يومي اختياري

### 3. سجل الإشعارات
- تتبع جميع الإشعارات المرسلة
- معرفة حالة كل إشعار (تم الإرسال / فشل)
- عرض تفاصيل الأخطاء في حالة الفشل

## كيفية الإعداد

### الخطوة 1: تشغيل السكريبت
قم بتشغيل السكريبت التالي لإنشاء الجداول المطلوبة:
\`\`\`sql
scripts/create-notification-tables.sql
\`\`\`

### الخطوة 2: إعداد Twilio
تأكد من إضافة متغيرات البيئة التالية:
- `TWILIO_ACCOUNT_SID`
- `TWILIO_AUTH_TOKEN`
- `TWILIO_WHATSAPP_NUMBER`

### الخطوة 3: تفعيل الإشعارات
1. انتقل إلى "إعدادات إشعارات WhatsApp" من القائمة
2. قم بتفعيل النظام
3. أضف أرقام الهواتف التي ستتلقى الإشعارات
4. خصص قالب الرسالة حسب الحاجة
5. احفظ الإعدادات

## API Endpoints

### 1. `/api/inventory/check-and-notify` (POST)
يفحص المخزون ويرسل إشعارات للمنتجات التي وصلت لنقطة إعادة الطلب.

**Parameters:**
\`\`\`json
{
  "product_id": 123,  // اختياري - لفحص منتج محدد
  "check_all": true   // اختياري - لفحص جميع المنتجات
}
\`\`\`

### 2. `/api/inventory/update-stock` (POST)
يحدث المخزون ويفحص تلقائياً إذا وصل المنتج لنقطة إعادة الطلب.

**Parameters:**
\`\`\`json
{
  "product_id": 123,
  "quantity_change": -10,
  "transaction_type": "sale",
  "notes": "بيع للعميل",
  "reference_type": "sales_order",
  "reference_id": 456
}
\`\`\`

### 3. `/api/inventory/send-reorder-notifications` (POST)
إرسال يدوي للإشعارات لجميع المنتجات التي تحتاج إعادة طلب.

## قالب الرسالة

يمكنك استخدام المتغيرات التالية في قالب الرسالة:
- `{product_name}` - اسم المنتج
- `{product_code}` - كود المنتج
- `{current_stock}` - المخزون الحالي
- `{reorder_point}` - نقطة إعادة الطلب

**مثال:**
\`\`\`
🔔 *تنبيه إعادة طلب*

📦 *المنتج:* {product_name}
🔢 *الكود:* {product_code}
📊 *المخزون الحالي:* {current_stock}
⚠️ *نقطة إعادة الطلب:* {reorder_point}

✅ يرجى اتخاذ الإجراء اللازم لإعادة طلب هذا المنتج.
\`\`\`

## الأمان والخصوصية

- يتم تشفير بيانات Twilio في متغيرات البيئة
- لا يتم إرسال إشعارات مكررة خلال 24 ساعة
- يتم تسجيل جميع الإشعارات للمراجعة والتدقيق
- يمكن تعطيل النظام بالكامل في أي وقت

## استكشاف الأخطاء

### لا يتم إرسال الإشعارات
1. تحقق من تفعيل النظام في الإعدادات
2. تأكد من إضافة أرقام هواتف صحيحة
3. تحقق من متغيرات البيئة الخاصة بـ Twilio
4. راجع سجل الإشعارات لمعرفة الأخطاء

### رسائل خطأ من Twilio
- تأكد من صحة رقم WhatsApp الخاص بـ Twilio
- تحقق من رصيد حساب Twilio
- تأكد من أن الأرقام المستقبلة بالصيغة الدولية (+966...)

## الدعم الفني
للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق الدعم الفني.
